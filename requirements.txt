# Core scientific stack
numpy>=1.24
scipy>=1.10          # statistical distributions & numerical routines
matplotlib>=3.8      # plotting utilities & figure generation

# Machine-learning / RL
torch>=2.2           # neural-network back-ends in rl.py
gymnasium[classic-control]>=0.29
tqdm>=4.66           # progress bars in training loops
scikit-learn>=1.3    # GaussianMixture demo in basics.ipynb

# Media / image handling
pillow>=10.0         # PIL.Image, used for figure composition in figures.ipynb
imageio>=2.31        # MP4 -> GIF conversion in basics.ipynb

# Parallelism
multiprocess>=0.70   # parallel RL repetitions in figures.ipynb

# Jupyter
jupyterlab>=4.1  # full web UI; brings in notebook 7.x
ipykernel>=6.29  # kernel that drives *.ipynb cells
ipywidgets>=8.0  # interactive widgets in notebooks

# Testing
pytest>=7.4    # unit tests

# Real-time COIN model. The Python package lives on the python-translation branch; main is the 
# MATLAB reference - the @ref is mandatory, since the default branch has no pyproject.toml.
realtimecoin @ git+https://github.com/RichieRich2569/real_time_md_coin.git@py-v0.1.0
# Local dev instead:
#   pip install -e "../real_time_md_coin/python"
